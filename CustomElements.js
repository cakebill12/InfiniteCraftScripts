javascript:(function() {    var name = prompt("Enter the name of the element:");    var emoji = prompt("Enter the emoji for the element:");    var discovered = confirm("Has the element been discovered?");        var newElement = {        text: name,        emoji: emoji,        discovered: discovered    };        var existingElements = localStorage.getItem("infinite-craft-data");    var elementsData;        if (existingElements) {        elementsData = JSON.parse(existingElements);                if (!elementsData.hasOwnProperty(%27elements%27)) {            elementsData.elements = [];        }                elementsData.elements.push(newElement);    } else {        elementsData = { elements: [newElement] };    }        localStorage.setItem("infinite-craft-data", JSON.stringify(elementsData));    window.location.reload();})();
